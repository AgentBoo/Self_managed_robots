<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title> Robotic Force </title>
    <link rel="stylesheet" href="../public/index.css">
  </head>

  <body>
    <header class="c-header f-header s-header">
      <div>
        <a href = "/fantastic/robots" class="c-header--a f-header--a s-header--a"> Currently Employed </a>
        <a href = "/great/robots" class="c-header--a f-header--a s-header--a"> Looking for Job </a>
      </div>
      <form class="c-credForm" method="post">
        <button class="btn btnLogin" type="submit" formaction="/login"> Log In </button>
        <button class="btn btnLogin" type="submit" formaction="/signup"> Sign up </button>
      </form>
    </header>


    <main>
      <div class="c-docTitle f-docTitle">
        <hr>
        <h1 class="c-docTitle__title"> User Index </h1>
        <hr>
      </div>

      <div class="c-indexRobots">
        {{#all}}
        <form>
          <!-- <button type="submit" formaction="/users" formmethod="get" name="robot" value="{{username}}" class="c-indexRobots__robots f-indexRobots__robots s-indexRobots__robots"> -->
          <button type="submit" formaction="/users/{{username}}" formmethod="get" class="c-indexRobots__robots f-indexRobots__robots s-indexRobots__robots">
            <img src="{{avatar}}">
            <h3 class="f-indexRobots__robotsName"> {{name}} </h3>
            <p> {{job}} {{company}} <span> {{^job}} Available for Hire {{/job}} </span></p>
          </button>
        </form>
        {{/all}}
      </div>
    </main>





  </body>
</html>
